import assert from'node:assert/strict';
import fs from'node:fs';
import path from'node:path';
import{COLLAB_ROLES,canCollaborate,campaignSharedProjection,campaignSharedCoreProjection,sessionSharedProjection,revealedAdventureProjection,adventureParticipantCharacterIds,adventureSharedRecord,collaborationMemberBundle,mergeNewest,normalizeUsername,usernameFromTechnicalEmail,normalizeAccount}from'../scripts/collaboration-model.js';
import{STORAGE_REGISTRY,COLLAB_CACHE_KEY}from'../scripts/storage-registry.js';

assert.deepEqual(COLLAB_ROLES,['dm','player','observer']);
assert.equal(normalizeUsername(' Rafael '),'rafael');
assert.equal(usernameFromTechnicalEmail('rafael@teias.invalid'),'rafael');
assert.deepEqual(normalizeAccount({uid:'u1',email:'Rafael@teias.invalid'},{domain:'teias.invalid'}),{uid:'u1',username:'rafael',displayName:'rafael'});
assert.equal(canCollaborate({ownerId:'u1',uid:'u1'},'adventure:write'),true);
assert.equal(canCollaborate({ownerId:'u1',uid:'u2',membership:{uid:'u2',role:'dm',active:true}},'encounter:write'),true);
assert.equal(canCollaborate({ownerId:'u1',uid:'u2',membership:{uid:'u2',role:'player',active:true}},'campaign:write'),false,'Jogador não edita Campanha.');
assert.equal(canCollaborate({ownerId:'u1',uid:'u2',membership:{uid:'u2',role:'player',active:true}},'character:write-own'),true,'Jogador pode editar a própria ficha.');

const campaign={id:'c1',name:'Mesa',dmNotes:'CONTEÚDO DO MESTRE',sharedNotes:'Visível',members:[{id:'m1',name:'Ana',role:'player',characterId:'pc1',notes:'NOTA DO MESTRE'},{id:'m2',name:'Beto',role:'player',characterId:'pc2'}],sessions:[{id:'s1',number:1,title:'Sessão Ana',sharedNotes:'Resumo A',dmNotes:'SEGREDO A',participantCharacterIds:['pc1']},{id:'s2',number:2,title:'Sessão Beto',sharedNotes:'Resumo B',dmNotes:'SEGREDO B',participantCharacterIds:['pc2']}],updatedAt:'2026-09-05T00:00:00Z'};
const shared=campaignSharedProjection(campaign),serialized=JSON.stringify(shared);assert.equal(serialized.includes('CONTEÚDO'),false);assert.equal(serialized.includes('NOTA DO MESTRE'),false);assert.equal(serialized.includes('SEGREDO'),false);assert.equal(shared.sessions.length,2);
const core=campaignSharedCoreProjection(campaign);assert.deepEqual(core.sessions,[],'Pacote comum da Campanha não pode carregar Sessões de outros participantes.');
const s1=sessionSharedProjection(campaign.sessions[0]);assert.deepEqual(s1.participantCharacterIds,['pc1']);
const adventures=[
 {id:'a1',campaignId:'c1',title:'Aventura Ana',scenes:[{id:'scene1',sessionId:'s1'}],handouts:[{id:'h1',title:'Revelado',revealed:true,content:'Mapa'},{id:'h2',title:'Oculto',revealed:false,content:'MESTRE HANDOUT'}],clues:[{id:'p1',title:'Achada',status:'discovered',text:'Pegadas'},{id:'p2',title:'Oculta',status:'hidden',text:'MESTRE PISTA'}]},
 {id:'a2',campaignId:'c1',title:'Aventura Beto',scenes:[{id:'scene2',sessionId:'s2'}],handouts:[],clues:[]}
];
assert.deepEqual(adventureParticipantCharacterIds(adventures[0],campaign),['pc1']);
assert.deepEqual(adventureParticipantCharacterIds(adventures[1],campaign),['pc2']);
const publicAdventure=revealedAdventureProjection(adventures[0]),publicJson=JSON.stringify(publicAdventure);assert.equal(publicJson.includes('MESTRE'),false);assert.deepEqual(publicAdventure.handouts.map(x=>x.id),['h1']);assert.deepEqual(publicAdventure.clues.map(x=>x.id),['p1']);
const record=adventureSharedRecord(adventures[0],campaign);assert.deepEqual(record.participantCharacterIds,['pc1']);
const memberBundle=collaborationMemberBundle({campaign:core},[{payload:s1}],[{payload:record.payload}]);assert.deepEqual(memberBundle.campaign.sessions.map(x=>x.id),['s1']);assert.deepEqual(memberBundle.revealedAdventures.map(x=>x.id),['a1']);
const newer={id:'x',updatedAt:'2026-09-05T12:00:00Z',value:2},older={id:'x',updatedAt:'2026-08-01T12:00:00Z',value:1};assert.equal(mergeNewest([newer],[older])[0].value,2);assert.equal(mergeNewest([older],[newer])[0].value,2);

const cache=STORAGE_REGISTRY.find(x=>x.key===COLLAB_CACHE_KEY);assert.equal(cache.scope,'cache');assert.equal(cache.portable,false);
const provider=fs.readFileSync(new URL('../scripts/firebase-collaboration-provider.js',import.meta.url),'utf8'),sync=fs.readFileSync(new URL('../scripts/collaboration-sync.js',import.meta.url),'utf8'),rules=fs.readFileSync(new URL('../firebase/firestore.rules',import.meta.url),'utf8'),indexes=fs.readFileSync(new URL('../firebase/firestore.indexes.json',import.meta.url),'utf8'),ui=fs.readFileSync(new URL('../scripts/collaboration-ui.js',import.meta.url),'utf8'),page=fs.readFileSync(new URL('../usuarios.html',import.meta.url),'utf8'),config=JSON.parse(fs.readFileSync(new URL('../dados/firebase-config.json',import.meta.url),'utf8')),firebaseJson=JSON.parse(fs.readFileSync(new URL('../firebase.json',import.meta.url),'utf8'));
assert.ok(provider.includes('firebasejs/12.18.0')||provider.includes("version='12.18.0'"));for(const token of['signInUsername','signInWithEmailAndPassword','listUsers','upsertMembership','saveOwnCharacter','saveCampaignBundle','getCampaignBundle','listMemberships','saveCampaignCharacter','listCampaignCharacters','array-contains'])assert.ok(provider.includes(token),`Provedor Firebase sem ${token}`);for(const token of['syncOwnCharacters','publishLocalCampaign','pullCollaborations','syncAll'])assert.ok(sync.includes(token),`Orquestração sem ${token}`);
for(const forbidden of['authorizedUsers','upsertAuthorizedUser','setAuthorizedUserActive','isAdmin'])assert.equal(provider.includes(forbidden),false,`Camada global removida ainda presente: ${forbidden}`);
for(const token of['isMember','isDm','canManage','assignedSharedRecord','linkedCharacter','adventureViews','participantCharacterIds'])assert.ok(rules.includes(token),`Rules sem ${token}`);assert.equal(rules.includes('allow read, write: if request.auth != null;'),false,'Acesso irrestrito não pode retornar.');assert.equal(rules.includes('authorizedUsers'),false);assert.equal(rules.includes('isAdmin'),false);
assert.ok(page.includes('Usuários e Colaboração')&&page.includes('login-username')&&page.includes('membership-role')&&page.includes('membership-character')&&page.includes('master-publish-tools'));assert.equal(page.includes('type="email"'),false);assert.equal(page.includes('Entrar com Google'),false);assert.ok(ui.includes('signInUsername')&&ui.includes('isMaster')&&ui.includes('characterId'));assert.equal(ui.includes('data-link-character'),false,'Jogador não deve alterar o próprio vínculo de Mesa.');
assert.equal(config.enabled,true);assert.equal(config.authMode,'username-password');assert.equal(config.usernameDomain,'teias.invalid');assert.equal(config.accountProvisioning,'firebase-console-manual');assert.equal(config.collaborationModel,'trusted-private');assert.equal(config.accessModel,'membership-character-scoped');assert.equal(config.roleVisibility,'firestore-and-application');for(const key of['projectId','apiKey','authDomain','appId'])assert.ok(String(config[key]||'').trim(),`Configuração real sem ${key}`);
assert.equal(config.platform,'web-site-only');assert.equal(config.hosting,'github-pages');assert.equal(config.firebasePlan,'spark');assert.equal(config.billingForbidden,true);assert.equal(config.paymentMethodForbidden,true);assert.equal('hosting'in firebaseJson,false);assert.equal('functions'in firebaseJson,false);
const repoRoot=new URL('../',import.meta.url).pathname,forbiddenMobile=[];function walk(dir){for(const entry of fs.readdirSync(dir,{withFileTypes:true})){if(entry.name==='.git')continue;const full=path.join(dir,entry.name);if(entry.isDirectory())walk(full);else if(['google-services.json','GoogleService-Info.plist'].includes(entry.name)||/\.apk$/i.test(entry.name))forbiddenMobile.push(path.relative(repoRoot,full))}}walk(repoRoot);assert.deepEqual(forbiddenMobile,[]);
const runtime=[provider,sync,rules,indexes,ui,page,JSON.stringify(config),JSON.stringify(firebaseJson)].join('\n').toLowerCase();assert.equal(runtime.includes('supabase'),false);assert.equal(runtime.includes('signingoogle'),false);assert.equal(runtime.includes('createinvite'),false);assert.equal(/private[_ -]?key|serviceaccount|client_secret/.test(JSON.stringify(config).toLowerCase()),false);
console.log('OK — colaboração por atribuição: Jogador recebe somente Mesa/Sessões/Aventuras ligadas ao personagem e escreve apenas a própria ficha.');
