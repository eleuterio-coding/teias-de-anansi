name: Arquivado - area Classes pdf24_merged

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "A publicacao foi concluida diretamente; este workflow esta arquivado."
