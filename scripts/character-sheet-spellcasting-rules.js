import{arr,num,fold}from'./character-builder/state.js';

const BREAK_CONCENTRATION=new Set(['incapacitado','atordoado','paralisado','inconsciente','petrificado']);
const SPECIFIC_MATERIAL=/(?:\b\d[\d.,]*\s*(?:gp|po|sp|pp|cp|pc|ep|pe)\b|consum(?:e|ed|ido|ida|ir|ível)|diamante|diamond|pérola|pearl)/i;
const now=()=>new Date().toISOString();
export function ensureSpellcastingState(character){if(!character)return null;character.sheet=character.sheet||{};let root=character.sheet.spellcasting;if(!root||typeof root!=='object'||Array.isArray(root))root={};root.concentration=root.concentration&&typeof root.concentration==='object'&&!Array.isArray(root.concentration)?root.concentration:null;root.concentrationHistory=arr(root.concentrationHistory).filter(Boolean);root.ritualCastLog=arr(root.ritualCastLog).filter(Boolean);character.sheet.spellcasting=root;return root}
export function spellComponentProfile(spell){const c=spell?.components||{},verbal=!!c.verbal,somatic=!!c.somatic,material=!!c.material,materialText=String(c.materialText||'').trim();return{verbal,somatic,material,materialText,specificMaterial:material&&SPECIFIC_MATERIAL.test(materialText),label:[verbal?'V':null,somatic?'S':null,material?'M':null].filter(Boolean).join(', ')||'—'}}
export function concentrationDc(damage){return Math.min(30,Math.max(10,Math.floor(Math.max(0,num(damage))/2)))}
export function activeConcentration(character){return ensureSpellcastingState(character)?.concentration||null}
export function startConcentration(character,spell,sourceKey='class'){const root=ensureSpellcastingState(character);if(!root||!spell?.concentration)return{ok:false,reason:'A magia não exige Concentração.'};const previous=root.concentration?{...root.concentration}:null;if(previous)root.concentrationHistory.push({...previous,endedAt:now(),endReason:'substituída por outra Concentração'});root.concentration={spellId:spell.id||null,spellName:spell.name||'Magia',sourceKey:String(sourceKey||'class'),startedAt:now()};return{ok:true,previous,current:root.concentration}}
export function endConcentration(character,reason='encerrada manualmente'){const root=ensureSpellcastingState(character),current=root?.concentration;if(!current)return{ok:false,reason:'Nenhuma Concentração ativa.'};root.concentrationHistory.push({...current,endedAt:now(),endReason:String(reason||'encerrada')});root.concentration=null;return{ok:true,ended:current}}
export function concentrationBreakingConditions(character){const conditions=arr(character?.sheet?.runtime?.conditions).map(fold),found=conditions.filter(condition=>BREAK_CONCENTRATION.has(condition));return[...new Set(found)]}
export function enforceConcentrationState(character){const active=activeConcentration(character),breaking=concentrationBreakingConditions(character);if(active&&breaking.length)return endConcentration(character,`condição incapacitante: ${breaking.join(', ')}`);return{ok:true,active,breaking}}
export function spellCastReadiness(character,spell,{bothHandsOccupied=false,warCaster=false}={}){const components=spellComponentProfile(spell),conditions=concentrationBreakingConditions(character),blocked=[],warnings=[];if(conditions.length)blocked.push('Uma condição incapacitante impede conjurar esta magia.');if(components.somatic&&bothHandsOccupied&&!warCaster&&!components.material)blocked.push('O componente Somático exige uma mão disponível; War Caster pode ignorar esta restrição.');if(components.material&&bothHandsOccupied&&!warCaster)warnings.push('Confirme que uma das mãos ocupadas está usando o foco ou o componente Material permitido.');if(components.specificMaterial)warnings.push(`Componente Material específico deve ser confirmado: ${components.materialText||'consulte a magia'}.`);if(components.verbal)warnings.push('Componente Verbal exige que o personagem consiga falar.');return{available:blocked.length===0,blocked,warnings,components}}
export function ritualEligible(spell,{prepared=false,wizardSpellbook=false}={}){return!!(spell?.ritual&&(prepared||wizardSpellbook))}
export function recordRitualCast(character,spell,sourceKey='class'){const root=ensureSpellcastingState(character);if(!spell?.ritual)return{ok:false,reason:'Esta magia não possui a tag Ritual.'};const row={id:`ritual-${Date.now()}-${Math.random().toString(36).slice(2,7)}`,spellId:spell.id||null,spellName:spell.name||'Magia',sourceKey:String(sourceKey||'class'),at:now()};root.ritualCastLog.push(row);return{ok:true,row}}
