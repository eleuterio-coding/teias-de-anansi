(()=>{
'use strict';

const VERSION='20260820-srd521-regras4';
const DDB_GLOSSARY='https://www.dndbeyond.com/sources/dnd/br-2024/rules-glossary';
const SRD_PDF='https://media.dndbeyond.com/compendium-images/srd/5.2/SRD_CC_v5.2.1.pdf';
const SRD_SOURCES=[
  'https://raw.githubusercontent.com/downfallx/dnd-5e-srd-markdown/1b4b99dcb786cdd1a2fb26f8acec1551191f1ca4/rules-glossary.md',
  'https://cdn.jsdelivr.net/gh/downfallx/dnd-5e-srd-markdown@1b4b99dcb786cdd1a2fb26f8acec1551191f1ca4/rules-glossary.md',
  'https://raw.githubusercontent.com/downfallx/dnd-5e-srd-markdown/master/rules-glossary.md'
];
const ROUTES={
  'Regras':'regras.html','Classes':'classes.html','Subclasses':'subclasses.html','Espécies':'especies.html',
  'Antecedentes':'antecedentes.html','Talentos':'talentos.html','Maestrias de Arma':'maestrias-de-arma.html',
  'Monstros':'monstros.html','Armaduras':'armaduras.html','Armas':'armas.html','Equipamentos de Aventura':'equipamentos-aventura.html',
  'Ferramentas':'ferramentas.html','Montarias e Veículos':'montarias-veiculos.html','Comércio e Despesas':'comercio-e-despesas.html',
  'Bugigangas':'bugigangas.html','Itens Mágicos':'itens-magicos.html','Magias':'magias.html','Idiomas':'idiomas.html'
};
const SPECIAL_LABELS={'Armaduras::#phb-2024-shield':'Escudo'};

const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const norm=v=>String(v??'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/\s+/g,' ').trim();
const slug=v=>String(v??'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/\[[^\]]+\]/g,'').replace(/[^A-Za-z0-9]+/g,'-').replace(/^-+|-+$/g,'').toLowerCase();
const originalFromHeading=h=>String(h||'').replace(/\s+\[(Action|Area of Effect|Attitude|Condition|Hazard)\]\s*$/i,'').trim();
const tagFromHeading=h=>((String(h||'').match(/\[(Action|Area of Effect|Attitude|Condition|Hazard)\]\s*$/i)||[])[1]||'').trim();
const familyPT=t=>({'Action':'Ação','Area of Effect':'Área de Efeito','Attitude':'Atitude','Condition':'Condição','Hazard':'Perigo'})[t]||'Regra Geral';

let PT_RULES=[],HOUSE=[],PATCHES=[],CURATED=null,INDEX=null,SRD=[],DDB_ONLY=[],SRD_SOURCE='';

function rawText(md){const holder=document.createElement('div');holder.innerHTML=String(md||'').replace(/<br\s*\/?>/gi,'\n');return holder.textContent.replace(/[_*`>#]/g,'').replace(/\n{3,}/g,'\n\n').trim()}
function extractEntries(markdown,knownOriginals){const lines=String(markdown||'').replace(/\r/g,'').split('\n'),starts=[];for(let i=0;i<lines.length;i++){const m=lines[i].match(/^####\s+(.+?)\s*$/);if(!m)continue;const heading=m[1].trim(),base=originalFromHeading(heading);if(knownOriginals.has(norm(base)))starts.push({i,heading,base,tag:tagFromHeading(heading)})}return starts.map((s,k)=>{const end=k+1<starts.length?starts[k+1].i:lines.length;return {heading:s.heading,original:s.base,tag:s.tag,markdown:lines.slice(s.i+1,end).join('\n').replace(/\n\* \* \*\s*$/,'').trim()}})}
function seeAlso(entry,allOriginals){const plain=rawText(entry.markdown),chunks=[],rx=/See also\b([\s\S]*?)(?=\n\n|$)/gi;let m;while((m=rx.exec(plain)))chunks.push(m[0]);const joined=chunks.join(' '),refs=[];for(const original of allOriginals){if(norm(original)===norm(entry.original))continue;const q=String(original).replace(/[.*+?^${}()|[\]\\]/g,'\\$&');if(new RegExp(`(^|[^A-Za-z])${q}([^A-Za-z]|$)`,'i').test(joined))refs.push(original)}return {refs}}
function ptRule(original){return PT_RULES.find(x=>norm(x.original)===norm(original))||null}
function ddbAnchor(heading){return String(heading||'').replace(/[^A-Za-z0-9]/g,'')}
function buildIndexMaps(){const byOriginal=new Map(),byId=new Map();for(const row of INDEX?.entidades||[]){if(row?.id)byId.set(row.id,row);if(row?.original)byOriginal.set(`${row.modulo}|${norm(row.original)}`,row)}return {byOriginal,byId}}
function fallbackRuleRow(original){const pt=ptRule(original);return {modulo:'Regras',original,nome:pt?.nome||original,id:null,url:'regras.html'}}
function entityForRule(original,maps){return maps.byOriginal.get(`Regras|${norm(original)}`)||fallbackRuleRow(original)}
function hrefFor(row){if(!row)return '#';if(row.modulo==='Regras'){if(row.id)return `regras.html?ref=${encodeURIComponent(row.id)}#ref-${encodeURIComponent(row.id)}`;return `regras.html#regra-${slug(row.original||row.nome)}`}const base=row.url||ROUTES[row.modulo]||'referencia.html';if(row.id&&base!=='referencia.html')return `${base}?ref=${encodeURIComponent(row.id)}#ref-${encodeURIComponent(row.id)}`;if(row.id)return `referencia.html?id=${encodeURIComponent(row.id)}`;return base}
function parseCurated(spec,maps){const s=String(spec||'').trim();if(!s)return null;if(!s.includes('::'))return maps.byOriginal.get(`Regras|${norm(s)}`)||fallbackRuleRow(s);const i=s.indexOf('::'),module=s.slice(0,i),value=s.slice(i+2);if(value.startsWith('#')){const id=value.slice(1),indexed=maps.byId.get(id);if(indexed?.modulo===module)return indexed;return {modulo:module,id,original:id,nome:SPECIAL_LABELS[s]||module,url:ROUTES[module]||'referencia.html'}}return maps.byOriginal.get(`${module}|${norm(value)}`)||{modulo:module,id:null,original:value,nome:value,url:ROUTES[module]||'referencia.html'}}
function semanticRefs(original,officialRefsList,maps){const specs=CURATED?.regras?.[original]||[],officialSet=new Set(officialRefsList.map(norm)),out=[],seen=new Set();for(const spec of specs){const row=parseCurated(spec,maps);if(!row)continue;if(row.modulo==='Regras'&&officialSet.has(norm(row.original)))continue;const key=row.id||`${row.modulo}|${norm(row.original)}`;if(seen.has(key))continue;seen.add(key);out.push(row)}return out}
function renderRefs(entry,maps,allOriginals){const off=seeAlso(entry,allOriginals);const officialLinks=off.refs.map(original=>{const row=entityForRule(original,maps),label=ptRule(original)?.nome||original;return `<a href="${esc(hrefFor(row))}">${esc(label)}</a>`}).join(' · ');const semantic=semanticRefs(entry.original,off.refs,maps);const semanticLinks=semantic.map(row=>`<a href="${esc(hrefFor(row))}">${esc(row.nome)} <small>(${esc(row.modulo)})</small></a>`).join(' · ');return `<div class="refs"><p><strong>Veja também — SRD 5.2.1:</strong> ${officialLinks||'<span class="muted">nenhuma referência de glossário explícita</span>'}</p><p><strong>Referências adicionais do Hub:</strong> ${semanticLinks||'<span class="muted">nenhuma adicional</span>'}</p></div>`}
function translatedBody(rule){if(!rule)return '<p class="muted">Sem tradução editorial local correspondente.</p>';const sections=(rule.secoes||[]).map(s=>`<section class="secao-regra"><h3>${esc(s.titulo)}</h3><p>${esc(s.texto)}</p></section>`).join('');return `<p>${esc(rule.descricao)}</p>${sections}`}
function renderOfficial(entry,maps,allOriginals){const rule=ptRule(entry.original),entity=maps.byOriginal.get(`Regras|${norm(entry.original)}`)||null,patch=PATCHES.find(x=>norm(x.original)===norm(entry.original)),family=entry.tag?familyPT(entry.tag):(rule?.familia||'Regra Geral'),cardId=entity?`ref-${entity.id}`:`regra-${slug(entry.original)}`,ddb=`${DDB_GLOSSARY}#${ddbAnchor(entry.heading)}`;return `<details class="item srd-item" id="${esc(cardId)}" data-hub-original="${esc(entry.original)}" ${entity?`data-hub-entity-id="${esc(entity.id)}"`:''}><summary><span><strong>${esc(rule?.nome||entry.original)}</strong><small class="original">${esc(entry.heading)}</small></span><span class="badge">${esc(family)}</span></summary><div class="corpo"><div class="source-strip"><span>SRD 5.2.1 · CC-BY-4.0</span><span>Tradução editorial PT-BR</span></div>${translatedBody(rule)}${patch?`<aside class="house-override"><strong>Precedência da Regra da Casa</strong><p>${esc(patch.descricao)}</p></aside>`:''}${renderRefs(entry,maps,allOriginals)}<div class="meta"><span><a href="${esc(ddb)}" target="_blank" rel="noopener noreferrer">D&D Beyond — Rules Glossary</a></span><span><a href="${SRD_PDF}" target="_blank" rel="noopener noreferrer">SRD 5.2.1 oficial</a></span></div></div></details>`}
function renderSupplemental(rule,maps){const entity=maps.byOriginal.get(`Regras|${norm(rule.original)}`)||null,cardId=entity?`ref-${entity.id}`:`regra-${slug(rule.original)}`,ddb=`${DDB_GLOSSARY}#${ddbAnchor(rule.original)}`,semantic=semanticRefs(rule.original,[],maps),semanticLinks=semantic.map(row=>`<a href="${esc(hrefFor(row))}">${esc(row.nome)} <small>(${esc(row.modulo)})</small></a>`).join(' · ');return `<details class="item ddb-only" id="${esc(cardId)}" data-hub-original="${esc(rule.original)}" ${entity?`data-hub-entity-id="${esc(entity.id)}"`:''}><summary><span><strong>${esc(rule.nome)}</strong><small class="original">${esc(rule.original)}</small></span><span class="badge">${esc(rule.familia||'Regra Geral')}</span></summary><div class="corpo"><div class="source-strip"><span>D&D Beyond Basic Rules 2024</span><span>Referência oficial complementar</span></div><p class="copyright-note">Consulte a fonte oficial para a redação integral desta regra.</p><div class="refs"><p><strong>Referências adicionais do Hub:</strong> ${semanticLinks||'<span class="muted">nenhuma adicional</span>'}</p></div><div class="meta"><span><a href="${esc(ddb)}" target="_blank" rel="noopener noreferrer">D&D Beyond — Rules Glossary</a></span></div></div></details>`}
function renderHouse(rule,maps){const entity=maps.byOriginal.get(`Regras|${norm(rule.original)}`)||null,cardId=entity?`ref-${entity.id}`:`regra-${slug(rule.original)}`,sections=(rule.secoes||[]).map(s=>`<section class="secao-regra"><h3>${esc(s.titulo)}</h3><p>${esc(s.texto)}</p></section>`).join(''),fonte=rule.fonte||{};return `<details class="item house-item" id="${esc(cardId)}" data-hub-original="${esc(rule.original)}" ${entity?`data-hub-entity-id="${esc(entity.id)}"`:''}><summary><span><strong>${esc(rule.nome)}</strong><small class="original">${esc(rule.original)}</small></span><span class="badge">Regra da Casa</span></summary><div class="corpo"><p>${esc(rule.descricao)}</p>${sections}<div class="meta"><span><b>Fonte:</b> ${esc(fonte.livro||'Regra da Casa')}</span><span><b>Edição:</b> ${esc(fonte.edicao||'')}</span></div></div></details>`}
function allCards(){const maps=buildIndexMaps(),originals=SRD.map(x=>x.original);return [...SRD.map(x=>({kind:'srd',original:x.original,name:ptRule(x.original)?.nome||x.original,family:x.tag?familyPT(x.tag):(ptRule(x.original)?.familia||'Regra Geral'),html:renderOfficial(x,maps,originals)})),...DDB_ONLY.map(x=>({kind:'ddb',original:x.original,name:x.nome,family:x.familia||'Regra Geral',html:renderSupplemental(x,maps)})),...HOUSE.map(x=>({kind:'house',original:x.original,name:x.nome,family:'Regra da Casa',html:renderHouse(x,maps)}))]}
function render(){const q=norm(document.getElementById('busca').value),source=document.getElementById('fonte').value,family=document.getElementById('familia').value,cards=allCards().filter(c=>(!q||norm(`${c.name} ${c.original} ${c.family}`).includes(q))&&(!source||c.kind===source)&&(!family||c.family===family));document.getElementById('resultado').textContent=`${cards.length} de ${SRD.length+DDB_ONLY.length+HOUSE.length} itens`;document.getElementById('lista').innerHTML=cards.map(c=>c.html).join('')||'<p class="nota">Nenhuma regra corresponde aos filtros.</p>';focusRequested()}
function focusRequested(){const id=new URLSearchParams(location.search).get('ref');if(id){const el=document.querySelector(`[data-hub-entity-id="${CSS.escape(id)}"]`);if(el){if(el.tagName==='DETAILS')el.open=true;el.classList.add('hub-ref-target');setTimeout(()=>el.scrollIntoView({behavior:'smooth',block:'center'}),80);return}}if(location.hash){const el=document.querySelector(location.hash);if(el){if(el.tagName==='DETAILS')el.open=true;setTimeout(()=>el.scrollIntoView({behavior:'smooth',block:'center'}),40)}}}
async function fetchRequired(url,type='json'){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error(`Falha de fonte HTTP ${r.status}: ${url}`);return type==='text'?r.text():r.json()}
async function fetchOptionalJson(url){try{const r=await fetch(url,{cache:'no-store'});return r.ok?await r.json():null}catch{return null}}
async function fetchSrd(){const errors=[];for(const url of SRD_SOURCES){try{const r=await fetch(url,{cache:'no-store'});if(r.ok){SRD_SOURCE=url;return await r.text()}errors.push(`${r.status} ${url}`)}catch(e){errors.push(`${e?.message||e} ${url}`)}}throw new Error(`Não foi possível carregar o SRD 5.2.1. ${errors.join(' | ')}`)}
async function load(){const partUrls=[1,2,3,4].map(n=>`dados/regras-atuais-ptbr.part${n}.txt?v=${VERSION}`),[partTexts,hub,extra,curated,markdown,index]=await Promise.all([Promise.all(partUrls.map(u=>fetchRequired(u,'text'))),fetchRequired(`dados/regras-hub.json?v=${VERSION}`),fetchRequired(`dados/regras-casa-adicionais.json?v=${VERSION}`),fetchRequired(`dados/referencias-regras-curadas.json?v=${VERSION}`),fetchSrd(),fetchOptionalJson(`dados/referencias-hub-index.json?v=${VERSION}`)]),data=JSON.parse(partTexts.join(''));CURATED=curated;INDEX=index||{entidades:[]};PT_RULES=[...(data.itens||[])];PATCHES=[...(hub.sobreposicoes||[])];HOUSE=[...(hub.itens||[]),...(extra.itens||[])];const known=new Set(PT_RULES.map(x=>norm(x.original)));SRD=extractEntries(markdown,known);const srdSet=new Set(SRD.map(x=>norm(x.original)));DDB_ONLY=PT_RULES.filter(x=>!srdSet.has(norm(x.original)));if(PT_RULES.length!==155)throw new Error(`Regras atuais inesperadas: ${PT_RULES.length}/155`);if(HOUSE.length!==4)throw new Error(`Regras da Casa inesperadas: ${HOUSE.length}/4`);if(SRD.length<130)throw new Error(`Extração SRD incompleta: ${SRD.length}`);document.getElementById('cont-srd').textContent=SRD.length;document.getElementById('cont-ddb').textContent=DDB_ONLY.length;document.getElementById('cont-house').textContent=HOUSE.length;const families=[...new Set([...SRD.map(x=>x.tag?familyPT(x.tag):(ptRule(x.original)?.familia||'Regra Geral')),...DDB_ONLY.map(x=>x.familia||'Regra Geral'),'Regra da Casa'])].sort((a,b)=>a.localeCompare(b,'pt-BR'));document.getElementById('familia').innerHTML='<option value="">Todas as famílias</option>'+families.map(f=>`<option>${esc(f)}</option>`).join('');render();document.documentElement.dataset.regrasFonte='srd-5.2.1';document.documentElement.dataset.regrasSrd=String(SRD.length);document.documentElement.dataset.regrasDdbComplementares=String(DDB_ONLY.length);document.documentElement.dataset.regrasCasa=String(HOUSE.length);document.documentElement.dataset.regrasIndice=INDEX?.entidades?.length?'disponivel':'fallback-local';document.documentElement.dataset.regrasSrdOrigem=SRD_SOURCE}
document.getElementById('busca').addEventListener('input',render);document.getElementById('fonte').addEventListener('change',render);document.getElementById('familia').addEventListener('change',render);load().catch(err=>{document.getElementById('resultado').textContent='Falha ao reconstruir o catálogo.';document.getElementById('lista').innerHTML=`<p class="erro"><strong>Falha:</strong> ${esc(err.message)}</p>`;console.error('[Regras SRD 5.2.1]',err)});
})();