import{read,write}from'./character-builder/state.js';
import{playerMode,assignedCharacterIds}from'./collaboration-view.js?v=20260905-access2';
const $=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const restricted=playerMode(),allowed=new Set(assignedCharacterIds());
function items(){const rows=read();return(restricted?rows.filter(c=>allowed.has(c.id)):rows).sort((a,b)=>String(b.updatedAt||'').localeCompare(String(a.updatedAt||'')))}
function render(){const rows=items(),el=$('lista');if(!el)return;if($('create-character-action'))$('create-character-action').hidden=restricted;if(!rows.length){el.innerHTML=`<div class="empty">${restricted?'Nenhuma ficha foi atribuída a esta conta. Sincronize em Usuários e Colaboração depois que o Mestre fizer o vínculo.':'Nenhum personagem salvo ainda. Use <strong>Criar personagem</strong> para iniciar.'}</div>`;return}el.innerHTML=`<div class="list">${rows.map(c=>{const s=c.refSnapshots||{},sub=[s.class?.name,c.choices?.class?.level?`nível ${c.choices.class.level}`:null,s.subclass?.name,s.species?.name,s.background?.name].filter(Boolean).join(' · ');return`<article class="pc"><div><h2>${esc(c.name||'Personagem sem nome')}</h2><div class="muted">${esc(sub||'Referências ainda não definidas')}</div>${restricted?'<div class="muted">Ficha atribuída a esta conta</div>':''}</div><div class="actions"><a class="btn primary" href="ficha-personagem.html?v=20260905-access2&id=${encodeURIComponent(c.id)}">Abrir Ficha</a>${restricted?'':`<a class="btn" data-structure-edit href="criacao-personagem.html?v=20260901-ux-final1&id=${encodeURIComponent(c.id)}">Editar estrutura</a><button class="btn danger" data-delete="${esc(c.id)}" type="button">Excluir</button>`}</div></article>`}).join('')}</div>`;if(!restricted)el.querySelectorAll('[data-delete]').forEach(b=>b.addEventListener('click',()=>{if(!confirm('Excluir este personagem deste navegador?'))return;write(read().filter(c=>c.id!==b.dataset.delete));render()}))}
render();