import{state,$,arr,num}from'./state.js';
import{selected,item,spellOptions,spellCreditState,canSelectLeveledSpell}from'./rules.js';

function selectedByLevel(ids){const counts={};for(const id of arr(ids)){const spell=item('spells',id),level=num(spell?.level);if(level>0)counts[level]=num(counts[level])+1}return counts}
function setHtml(el,html){if(el&&el.innerHTML!==html)el.innerHTML=html}
function setText(el,text){if(el&&el.textContent!==text)el.textContent=text}
function remainingForLevel(credits,spellLevel){return arr(credits?.remaining).filter(maxLevel=>num(maxLevel)>=spellLevel).length}

function decorateChoices(){const box=$('magias-escolhas'),{klass}=selected();if(!box||!klass?.spellAbility)return;const level=Math.max(1,Math.min(20,num(state.c?.choices?.class?.level)||1)),opts=spellOptions(klass,level),sel=state.c?.choices?.spells||{cantrips:[],leveled:[],arcanum:{}},counts=selectedByLevel(sel.leveled),credits=spellCreditState(klass,level,sel.leveled);
 const fieldset=box.querySelector('fieldset');if(fieldset){const legend=fieldset.querySelector(':scope>legend');setText(legend,klass.slug==='wizard'?'Magias da classe — Grimório':klass.slug==='warlock'?'Magias da classe — Pact Magic':'Magias da classe')}
 for(const details of box.querySelectorAll('details.spell-level')){const summary=details.querySelector(':scope>summary');if(!summary)continue;const text=summary.textContent||'';if(/^Truques/i.test(text)){const chosen=arr(sel.cantrips).length,need=num(opts.progress.cantrips);setHtml(summary,`<strong>Truques</strong> — ${chosen}/${need}`);continue}const m=text.match(/(\d+)º\s+nível/i);if(!m)continue;const spellLevel=num(m[1]),chosen=num(counts[spellLevel]),remaining=remainingForLevel(credits,spellLevel);setHtml(summary,`<strong>${spellLevel}º nível</strong> — ${chosen} escolhida${chosen===1?'':'s'} · faltam ${remaining}`);for(const input of details.querySelectorAll('input[data-kind="leveled"]'))input.disabled=!input.checked&&!canSelectLeveledSpell(klass,level,sel.leveled,input.value)}
 const totalLine=[...box.querySelectorAll('p.mini')].find(p=>/(Magias preparadas\/conhecidas|Magias no grimório|Magias de Pact Magic)/i.test(p.textContent||''));if(totalLine)setHtml(totalLine,`<strong>${opts.progress.selectionLabel}:</strong> ${arr(sel.leveled).length}/${opts.progress.selectionTotal}`);
 for(const select of box.querySelectorAll('.arcanum-select')){const spellLevel=num(select.dataset.level),label=select.closest('label');if(!label)continue;let marker=label.querySelector('.spell-quota-arcanum');if(!marker){marker=document.createElement('small');marker.className='mini spell-quota-arcanum';label.insertBefore(marker,select)}const chosen=sel.arcanum?.[spellLevel]?1:0;setText(marker,` — ${chosen}/1 `)}
}

function decorateSheet(){const box=$('spellcasting'),{klass}=selected();if(!box||!klass?.spellAbility)return;const level=Math.max(1,Math.min(20,num(state.c?.choices?.class?.level)||1)),opts=spellOptions(klass,level),sel=state.c?.choices?.spells||{leveled:[]};if(klass.slug!=='wizard')return;const row=[...box.querySelectorAll('.value-row')].find(r=>/Preparadas\/conhecidas/i.test(r.querySelector('span')?.textContent||''));if(!row)return;setText(row.querySelector('span'),'Grimório');setText(row.querySelector('strong'),`${arr(sel.leveled).length}/${opts.progress.selectionTotal}`);let prep=box.querySelector('[data-wizard-prepared-limit]');if(!prep){prep=document.createElement('div');prep.className='value-row';prep.dataset.wizardPreparedLimit='true';row.insertAdjacentElement('afterend',prep)}setHtml(prep,`<span>Limite de preparação</span><strong>${opts.progress.actualPrepared}</strong>`)}

function apply(){decorateChoices();decorateSheet()}
export function initSpellQuotaUi(){const root=$('builder');if(!root)return;apply();const observer=new MutationObserver(()=>apply());observer.observe(root,{childList:true,subtree:true});return observer}
