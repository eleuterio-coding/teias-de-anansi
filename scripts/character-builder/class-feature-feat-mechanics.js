import{state,arr,num,fold}from'./state.js';
import{featPrerequisiteOk,minimumFeatLevel}from'./feat-prerequisite-mechanics.js';
import{backgroundSpecialFeatInstance}from'./background-special-feat-mechanics.js';

const FEATURE_PATTERNS={fightingStyle:/^(fighting style|estilo de luta)$/,epicBoon:/^(epic boon|dadiva epica)$/};
function featureName(feature){return fold(feature?.originalName||feature?.name||'')}
export function activeClassFeatures(klass,level){const current=Math.max(1,Math.min(20,num(level)||1));return arr(klass?.features).filter(feature=>num(feature?.level)<=current)}
export function hasClassFeature(klass,level,kind){const pattern=FEATURE_PATTERNS[kind];return!!pattern&&activeClassFeatures(klass,level).some(feature=>pattern.test(featureName(feature)))}
export function classFeatureFeatGrants(klass,level){const grants=[];if(hasClassFeature(klass,level,'fightingStyle'))grants.push({kind:'fightingStyle',key:'class-feature:fighting-style',choiceKey:'fightingStyle',label:'Estilo de Luta',source:'Classe · Estilo de Luta'});if(hasClassFeature(klass,level,'epicBoon'))grants.push({kind:'epicBoon',key:'class-feature:epic-boon',choiceKey:'epicBoon',label:'Dádiva Épica ou outro talento elegível',source:'Classe · nível 19 · Epic Boon'});return grants}
function grantShapeEligible(feat,grant,klass,level){const current=Math.max(1,Math.min(20,num(level)||1));if(!feat||!grant)return false;if(grant.kind==='fightingStyle')return feat.category==='Estilo de Luta'&&hasClassFeature(klass,current,'fightingStyle');return grant.kind==='epicBoon'&&hasClassFeature(klass,current,'epicBoon')&&minimumFeatLevel(feat)<=current}
function normalizedContext(context,klass,level){const resolvedClassProficiencies=arr(context?.classProficiencies).length?arr(context.classProficiencies):[...arr(klass?.proficiencies),...arr(klass?.proficienciesRaw)];return{...context,klass,level,classProficiencies:resolvedClassProficiencies,featMechanics:context?.featMechanics||{armorTraining:arr(context?.featArmorTraining),shieldTraining:!!context?.featShieldTraining}}}
export function featEligibleForClassGrant(feat,grant,context={}){const klass=context.klass||null,level=context.level??1;if(!grantShapeEligible(feat,grant,klass,level))return false;return featPrerequisiteOk(feat,normalizedContext(context,klass,level))}
function currentClass(){return state.catalogs.classes.find(x=>x.id===state.c?.refs?.class)||null}
function choiceStore(){state.c.choices.classFeatureFeats=state.c.choices.classFeatureFeats||{};return state.c.choices.classFeatureFeats}
export function classFeatureFeatInstances(){if(!state.c?.choices)return[];const klass=currentClass(),level=num(state.c.choices.class?.level)||1,choices=choiceStore(),grants=classFeatureFeatGrants(klass,level),out=[];for(const grant of grants){const id=choices[grant.choiceKey],feat=state.catalogs.feats.find(x=>x.id===id);if(grantShapeEligible(feat,grant,klass,level))out.push({key:grant.key,feat,source:grant.source,classFeatureGrant:grant})}const special=backgroundSpecialFeatInstance();if(special)out.push(special);return out}
export function sanitizeClassFeatureFeatSelections(context={}){if(!state.c?.choices)return false;const klass=currentClass(),level=num(state.c.choices.class?.level)||1,grants=classFeatureFeatGrants(klass,level),allowed=new Map(grants.map(g=>[g.choiceKey,g])),choices=choiceStore(),rich=!!(context?.scores||context?.species||context?.featMechanics||context?.classProficiencies);let changed=false;for(const key of Object.keys(choices))if(!allowed.has(key)){delete choices[key];changed=true}for(const[key,grant]of allowed){const id=choices[key];if(!id)continue;const feat=state.catalogs.feats.find(x=>x.id===id),valid=rich?featEligibleForClassGrant(feat,grant,{klass,level,...context}):grantShapeEligible(feat,grant,klass,level);if(!valid){delete choices[key];changed=true}}return changed}
export function eligibleClassFeatureFeats(grant,context={}){const klass=context.klass||currentClass(),level=context.level??state.c?.choices?.class?.level??1;return state.catalogs.feats.filter(feat=>featEligibleForClassGrant(feat,grant,{klass,level,...context}))}
